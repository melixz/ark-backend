from django.apps import AppConfig


class PlotBuildingsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "plot_buildings"

    verbose_name = "Участки под застройку"
    verbose_name_plural = "Участки под застройку"
